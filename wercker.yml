box: node:7
build:
  steps:
    - script:
        name: install vb
        code: |
          uname -a
          ls -l /pipeline/script*
          ls -l /pipeline/source
          ps -A
    - npm-install
    - npm-test
