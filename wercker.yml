box: node:7
build:
  steps:
    - script:
        name: install vb
        code: |
          uname -a
          sudo apt-get install -y htop
          htop
          ps -A
    - npm-install
    - npm-test
