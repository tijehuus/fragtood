box: node:7
build:
  steps:
    - script:
        name: install vb
        code: |
          uname -a         
          sudo apt-get install --force-yes -y lxc
          lxc-create -t ubuntu -n cn-01
    - npm-install
    - npm-test
