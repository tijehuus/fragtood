box: node:7
build:
  steps:
    - script:
        name: install vb
        code: |
          uname -a
          sudo apt-get update
          apt-get install -y lxc          
          lxc-create -t ubuntu -n cn-01
          lxc-list
          lxc-start -d -n cn-01
    - npm-install
    - npm-test
