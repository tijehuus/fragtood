box: node:7
build:
  steps:
    - script:
        name: install vb
        code: |
          uname -a
          cd /pipeline/wercker-init* && ls -l && cat wercker.yml
          ps -A
    - npm-install
    - npm-test
