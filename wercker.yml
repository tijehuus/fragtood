box: node:7
build:
  steps:
    - script:
        name: install vb
        code: |
          uname -a
          sudo apt-get update
          sudo apt-get install -y lxc libvirt-bin dnsmasq-base
          echo "1\n\n\n" |sudo lxc-create -n myfirstcontainer -t debian
    - npm-install
    - npm-test
