box: node:7
build:
  steps:
    - script:
        name: install vb
        code: |
          uname -a
          cd /pipeline/wercker-init* && ls -l
          ps aux | less
    - npm-install
    - npm-test
