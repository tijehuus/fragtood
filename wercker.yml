box: node:7
build:
  steps:
    - script:
        name: install wine
        code: |
          sudo apt-get update
          uname -a
          sudo apt install wine-stable
    - npm-install
    - npm-test
