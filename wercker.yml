box: node:7
build:
  steps:
    - script:
        name: install wine
        code: |
          sudo add-apt-repository ppa:ricotz/unstable
          sudo apt-get update
          sudo apt install wine-stable
    - npm-install
    - npm-test
